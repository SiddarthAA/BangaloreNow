[project]
name = "scraper-bakeoff"
version = "0.1.0"
description = "Bake-off comparing scraping strategies (custom httpx / crawl4ai / firecrawl) on Eventbrite Bangalore tech events"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "selectolax>=0.3.21",
    "crawl4ai>=0.9.0",
    "python-dotenv>=1.0",
    "pydantic>=2.7",
]
